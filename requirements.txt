# Core dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Telegram bot
aiogram>=3.0.0

# AI/LLM
anthropic>=0.7.0

# MCP (Model Context Protocol)
mcp>=1.13.0

# Image processing
Pillow>=10.0.0

# Utilities
python-multipart>=0.0.6
python-dotenv>=1.0.0

# Standard library imports (built-in)
# os, re, json, time, asyncio, threading, tempfile, traceback
# html, ast, urllib.parse, datetime, typing, contextlib

# Development (optional)
pytest>=7.4.0
black>=23.0.0
flake8>=6.0.0 